{
  "name": "archivist_repo",
  "author": {
    "name": "Julius Pargac",
    "url": "https://github.com/blackfan23"
  },
  "private": false,
  "version": "1.0.0",
  "license": "GNU General Public License v3.0",
  "scripts": {
    "archivist:extract": "bun scripts/extract-archivist.ts ~/Developer/archivist",
    "archivist:win": "bunx @electron/rebuild --only better-sqlite3 --arch x64 && nx build archivist-frontend --prod && nx build archivist --prod && nx make archivist --platform=windows --arch=x64",
    "archivist:mac": "bunx @electron/rebuild --only better-sqlite3 --arch x64 && nx build archivist-frontend --prod && nx build archivist --prod && nx make archivist --platform=mac --arch=x64",
    "archivist:mac:make-only": "bunx @electron/rebuild --only better-sqlite3 --arch x64 && nx make archivist --platform=mac --arch=x64",
    "archivist:mac:arm64": "bunx @electron/rebuild --only better-sqlite3 --arch arm64 && nx build archivist-frontend --prod && nx build archivist --prod && nx make archivist --platform=mac --arch=arm64",
    "archivist:mac:arm64:make-only": "bunx @electron/rebuild --only better-sqlite3 --arch arm64 && nx make archivist --platform=mac --arch=arm64",
    "archivist:linux": "bunx @electron/rebuild --only better-sqlite3 --arch x64 && nx build archivist-frontend --prod && nx build archivist --prod && nx make archivist --platform=linux --arch=x64",
    "archivist:linux:arm64": "bunx @electron/rebuild --only better-sqlite3 --arch arm64 && nx build archivist-frontend --prod && nx build archivist --prod && nx make archivist --platform=linux --arch=arm64",
    "postinstall": "npx @electron/rebuild --only better-sqlite3"
  },
  "dependencies": {
    "@angular/common": "21.2.0",
    "@angular/compiler": "21.2.0",
    "@angular/core": "21.2.0",
    "@angular/forms": "21.2.0",
    "@angular/platform-browser": "21.2.0",
    "@angular/router": "21.2.0",
    "rxjs": "~7.8.0",
    "date-fns": "^4.1.0",
    "valibot": "^1.2.0",
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0",
    "electron-store": "^11.0.2",
    "better-sqlite3": "^12.6.2",
    "ollama": "^0.6.3",
    "openai": "^6.25.0",
    "@anthropic-ai/sdk": "^0.78.0",
    "@google/generative-ai": "^0.24.1",
    "fdir": "^6.5.0"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "21.2.0",
    "@angular-devkit/core": "21.2.0",
    "@angular-devkit/schematics": "21.2.0",
    "@angular/build": "21.2.0",
    "@angular/cli": "21.2.0",
    "@angular/compiler-cli": "21.2.0",
    "@angular/language-service": "21.2.0",
    "typescript": "~5.9.2",
    "typescript-eslint": "^8.56.1",
    "nx": "22.5.3",
    "@nx/angular": "22.5.3",
    "@nx/eslint": "22.5.3",
    "@nx/eslint-plugin": "22.5.3",
    "@nx/esbuild": "22.5.3",
    "@nx/js": "22.5.3",
    "@nx/workspace": "22.5.3",
    "eslint": "^9.8.0",
    "angular-eslint": "21.2.0",
    "electron": "40.6.1",
    "electron-builder": "26.8.1",
    "nx-electron": "22.0.0",
    "prettier": "^3.8.1"
  },
  "nx": {
    "includedScripts": []
  },
  "trustedDependencies": [
    "@fortawesome/fontawesome-common-types",
    "@fortawesome/fontawesome-free",
    "@fortawesome/fontawesome-svg-core",
    "@parcel/watcher",
    "@swc/core",
    "better-sqlite3",
    "canvas",
    "core-js",
    "electron",
    "electron-winstaller",
    "esbuild",
    "ffmpeg-static",
    "iconv",
    "lmdb",
    "msgpackr-extract",
    "nx",
    "pcsclite",
    "unrs-resolver"
  ]
}